# Copy to .env and fill in values.
# Required for workflow persistence (local or MongoDB Atlas).

# Local MongoDB (default port)
# MONGODB_URI=mongodb://localhost:27017/automation

# MongoDB Atlas (replace <user>, <password>, <cluster> with your values)
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/automation?retryWrites=true&w=majority
MONGODB_URI=
